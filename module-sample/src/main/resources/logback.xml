<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="60 seconds" debug="false">

    <conversionRule conversionWord="clr" converterClass="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="wex"
                    converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>
    <conversionRule conversionWord="wEx"
                    converterClass="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter"/>

    <property name="COMMON_LOG_PATTERN"
              value="%d %highlight([%t]) %-5p %c{0} - %m [%X{X-B3-TraceId:-},%X{X-B3-SpanId:-},%X{X-Span-Export:-}]%n"/>

    <!-- 项目名称 -->
    <property name="APP_NAME" value="base"/>
    <!-- 日志文件目录 -->
    <property name="LOG_PATH" value="C:/Users/59305/Desktop/project/test/log/${APP_NAME}"/>​
    <!-- 归档文件保留的时间，即只保留几天前的归档文件，单位与fileNamePattern中配置的最小时间单位相同 -->
    <property name="MAX_HISTORY" value="7"/>
    <!-- 当前日志文件达到maxFileSize后则归档 -->
    <property name="MAX_FILE_SIZE" value="1GB"/>
    <!-- 每一类日志的所有归档文件达到totalSizeCap后会异步删除最老的归档文件，比如common-error.log为一类，xxx-biz.log为一类 -->
    <property name="TOTAL_SIZE_CAP" value="10GB"/>

    <appender name="CONSOLE-APPENDER" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${COMMON_LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!--<appender name="TASK-APPENDER" class="ch.qos.logback.core.rolling.RollingFileAppender">-->
        <!--<encoder>-->
            <!--<pattern>${COMMON_LOG_PATTERN}</pattern>-->
        <!--</encoder>-->
        <!--<file>${LOG_PATH}/${APP_NAME}-task.log</file>-->
        <!--<rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">-->
            <!--<fileNamePattern>${LOG_PATH}/${APP_NAME}-task.log.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>-->
            <!--<maxHistory>${MAX_HISTORY}</maxHistory>-->
            <!--<maxFileSize>${MAX_FILE_SIZE}</maxFileSize>-->
            <!--&lt;!&ndash;<totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>&ndash;&gt;-->
        <!--</rollingPolicy>-->
    <!--</appender>-->

    <!--<Logger name="TASK_LOGGER" level="INFO" additivity="false">-->
        <!--<appender-ref ref="TASK-APPENDER"/>-->
    <!--</Logger>-->

    <root level="INFO">
        <appender-ref ref="CONSOLE-APPENDER"/>
    </root>

</configuration>